apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubesealdemo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubesealdemo
  template:
    metadata:
      labels:
        app: kubesealdemo
    spec:
      containers:
      - name: mysql
        image: mysql:latest
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysecret
              key: password
        ports:
        - containerPort: 3306
